/*
 * Moved to @onborn/billing, where the web funnel host shares it: the mapper
 * reads billing's own package type and nothing here was React Native's. The
 * name stays for the callers that learned it.
 */
export { createBuilderV2PlanSnapshot } from "@onborn/billing";
